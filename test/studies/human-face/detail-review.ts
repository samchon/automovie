import type * as Human from "@automovie/human";

/**
 * Detailed numerical editor source inspection.
 * Earlier inspections are retained; domain ownership does not imply a new
 * anatomical range or rendered improvement. The root carrier retains this domain.
 *
 * @evidence {@link Human.humanFaceDetailChannels} Maps the editor's detailed scalar inventory to real profile fields.
 * @evidenceReview {@link Human.humanFaceDetailChannels} #7e6251b Read every domain declaration and the shared metadata constructor against the prior inventory. All 107 evaluated channels retain their full values and display order, including the three unpaired mouth.cavityChamber paths beside the wall channel. The inherited-value reader, immutable writers and region panel still consume this inventory. Chamber expansions remain 0..30 and transition depth 0.1..60; editor bounds do not establish anatomy or coupled clearance.
 * @evidence {@link Human.humanFaceDetailValue} Reads the displayed scalar from the resolved common or side-specific anatomical profile.
 * @evidenceReview {@link Human.humanFaceDetailValue} #01290ac Read known-channel admission, full region resolution and field traversal. A missing optional profile returns undefined rather than inventing zero, while returned scalars reflect trait/detail precedence rather than merely echoing a draft slider.
 * @evidence {@link Human.IAutoMovieHumanFaceDetailChannel} Describes one detailed numerical control and its anatomical owner, inherited neutral and attachment.
 * @evidenceReview {@link Human.IAutoMovieHumanFaceDetailChannel} #c3c0eac Read the fields against the private panel and setters: path, units, minimum/maximum, step, side ownership and actual attachment description. Scalar range admission cannot certify that the resulting coupled geometry can be constructed.
 * @evidence {@link Human.setHumanFaceDetail} Writes or clears one precise override without flattening inherited part settings.
 * @evidenceReview {@link Human.setHumanFaceDetail} #ffe5f28 Read the setter and complete path helper after reproducing cross-owner aliases. Every existing path container is shallow-copied before descent, including arrays, so writes/removals cannot reach a shared basis or opposite profile. Missing paths still remain absent on removal, empty ancestors still prune, and finite/count/side admission is unchanged. This repairs the shared-object case that a whole structuredClone alone did not isolate.
 * @evidence {@link Human.setHumanFaceHairLayerDetail} Edits one explicitly named additional profile without re-entering its guide array or changing the legacy owner.
 * @evidenceReview {@link Human.setHumanFaceHairLayerDetail} #8f691d3 Read the setter and raw merge/path consumers after reproducing two layers sharing one profile. Raw composition retains authored omissions; path-local copies then detach the selected profile from sibling aliases. The corrected oracle clones each expected layer independently and directly asserts that the unselected shade remains omitted. Unknown/non-hair channels, invalid values and missing layers refuse; inheritance remains whole-array.
 *
 * @remarks
 * Internal domain inspection under the public inventory above. These owners
 * are consumed inside the package; the native package reference selects its
 * entry exports, so these notes make no independent fingerprint claim.
 *
 * - createHumanFaceDetailChannel: Read the extracted constructor beside every domain call: stable region/path ID, split path, declared units and bounds, basis neutral, eye/cheek/ear pairing and the unchanged attachment table. It only constructs channel metadata; the existing immutable setters and components retain admission. Every declaration is reached by the public inventory and its all-channel endpoint/refusal scenario.
 * - humanFaceNeckChannels: Read the submental projection envelope and its fixed endpoint/tangent meaning; the public inventory keeps its original first position.
 * - humanFaceCavityChannels: Read the straight-wall ratio and three chamber millimetre controls. All previous bounds, steps, field paths and coupled-clearance caveats are retained.
 * - humanFaceTongueChannels: Read the native tongue parameter mapping; its metadata and exact millimetre step feed the same public controls.
 * - humanFaceLashChannels: Read the native lash parameter mapping, upperLashProfile prefix and preservation of each parameter unit, interval and effect.
 * - humanFaceSkinChannels: Read the native skin parameter mapping and preservation of its owned bounds, steps and meanings.
 * - humanFaceHairChannels: Read all ten hair, curl, painted finish and sampling entries. Full paths, bounds, units and effects are copied unchanged; additional hair-layer editing uses the same public vocabulary.
 * - humanFaceLowerDentalChannels: Read the two mandibular arch dimensions and two placement controls against their original ranges and field paths; only inventory ownership changes.
 * - humanFaceFrameChannels: Read all eight foundation controls, including independent brow foundation and chin/temple directions. The common host owns their coupled geometry.
 * - humanFaceOcularChannels: Read all seventeen aperture, section, optical and brow declarations against their original literals. Scalar limits remain authoring envelopes and do not establish anatomical dimensions or clearance.
 * - humanFaceNasalChannels: Read all nine external/support/aperture controls and their signed units; nasal fitting still owns common exterior and cavity admission.
 * - humanFaceLipChannels: Read all ten aperture, commissure, contact and vermilion section controls. The same immutable leaf writer preserves unrelated overrides.
 * - humanFaceCheekChannels: Read the three regional support projections and nasolabial groove depth; bilateral ownership remains derived by the common constructor.
 * - humanFaceEarChannels: Read all five pinna outline and placement controls. Signed centre placement and paired ownership are unchanged.
 * - humanFaceUpperDentalChannels: Read all five maxillary arch and placement controls, including gap. Original ranges and order remain unchanged.
 */
export const humanFaceDetailReview = { scope: "detailed numerical editing" };
