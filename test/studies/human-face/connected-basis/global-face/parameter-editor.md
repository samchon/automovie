# Parameter editor investigation

Issue [#2533](https://github.com/samchon/automovie/issues/2533) requires a face document to contain numerical shape and expression weights, material overrides and resource identities. Per-vertex identity displacement and per-document corrective geometry violate that boundary. Geometry, shared corrective fields and attachment correspondence belong to the reusable basis.

## Acceptance surface

The user requires individual characters to be reproduced by numerical parameters and resource choices, without per-person vertex sculpting. Timing is not the deciding criterion. The current eighteen documents contain shape/expression weights, material values and registered hair/skin choices, plus identity and basis metadata. Hair and skin choices resolve authored resources; the resources themselves can contain geometry or images. On 2026-09-21 the agent incorrectly treated the representation milestone as completion of the whole mission. The user's subsequent instruction to continue supersedes that conclusion: the original fine-parameter, broad simple-control and anatomical work remains active in Draft PR #2534. Earlier completion comments describe a partial milestone, not acceptance of the face editor.

The investigation covers the connected editor, its numerical builder, static export and all eighteen published subjects. It includes neutral shape, unilateral and combined expression, intermediate weights, invalid input, interrupted edits, undo, redo and document replay. Frontal, both oblique, both profile, rear and clay observations are separate from numeric admission. A successful render or a sampled collision result does not prove an unrestricted anatomical guarantee.

Repeated evaluation must preserve correspondence and update resident position and normal buffers. Basis preparation, material/resource changes, explicit contact checks and export are measured separately. The original handoff requested less than 50 ms per warm pose or subject change and a five-second eighteen-subject, twelve-pose capture population. In the subsequent 2026-09-21 conversation the user accepted approximately one second for subject replacement and clarified that changing the numerical value itself is distinct from calculating and displaying the resulting face. Their reference to roughly 0–1 ms concerned the value change alone, not an observed end-to-end rendering time. This supersedes treating 50 ms as a required subject-replacement threshold; historical timing observations below retain their original measurement scope. The capture-population target remains unverified.

## Anatomical questions

### Rigid-component investigation, 2026-09-21

On `e166c64a`, actual builder output was sampled on the neutral plus eighteen compact documents at fifteen rest, jaw, tongue and gaze states. Independent NumPy SVD registration was checked against an analytic 90-degree rotation with translation and a contraction negative control. The worst mandibular proper-rigid fit residual was 0.477991 mm RMS for full opening and 0.598616 mm RMS for full opening with mouth closure. Lateral eye movement reached 0.166683 mm RMS over one globe. The maxillary selection remained stationary in this population. The neutral jaw's existing arc correctives already reduced the half-opening residual to approximately 0.0004 mm; that neutral result did not transfer to every edited identity.

The existing editor was also captured on all eighteen subjects at ten rest/jaw/tongue states in front, both obliques, both profiles and frontal clay, 1,080 frames in total. All eighteen contact sheets were inspected. Shared orbital ridges, thin neutral tooth exposure, strongly stretched full-opening lips, jagged neck truncation and detached or bunched grooms remain observations. These views establish a before population for the oral comparison; they do not cover source-photo registration or the full expression space. Local capture and measurement files are under `.shots/face-2533/articulation-views-before` and `articulation-before`.

The first candidate declares complete rigid components in the shared basis and registers their shape-only reference to their performed target with a proper rotation and translation. [Horn's absolute-orientation paper](https://people.csail.mit.edu/bkph/papers/Absolute_Orientation_Scanned.pdf), section 4, provides the symmetric quaternion eigenproblem; scale is held at one here. [Müller et al.'s shape-matching paper](https://matthias-research.github.io/pages/publications/MeshlessDeformations_SIG05.pdf), section 3.3, describes the related rigid goal-position formulation. This candidate does not adopt their dynamics, infer anatomical pivots, or establish contact validity. A direct shape-dependent joint trajectory remains a separate representation question. The candidate must be judged through the editor and neighboring oral surfaces before its data is published.

The implemented candidate preserves the mandibular shape to floating precision over the same neutral-plus-eighteen, fifteen-state population. Sixteen focused math/basis tests passed, and reversing the quaternion's Z component deliberately failed the analytic rotation assertion; restoration passed. Its actual builder positions and normals were rendered in the calibrated resident scene for the same 1,080 oral frames, and all eighteen sheets were inspected. Neighbouring buffers and static tooth correspondence were checked unchanged. This comparison injects candidate buffers into the existing display; it does not establish a migrated editor/export path.

The complete 180-state oral crossing comparison found no newly crossing tooth/neighbor pair and unchanged tooth/lip crossing counts. Existing crossings remain: skin counts increased in 24 states and decreased in 36; tongue counts increased in 40 and decreased in 32. The largest increases were 304 to 322 crossed triangles against skin at the generated white boy's full opening, and 517 to 604 against tongue at Miriam's opening-plus-closure combination. Counts include both surfaces, measure strict triangle crossings and do not identify acceptable embedded tissue or penetration depth. These results keep the coupled oral constraint work open; geometric rigidity alone is not anatomical acceptance. Candidate buffers, captures and full crossing records remain local under `.shots/face-2533/`.

The mandibular arch must remain rigid during performance. Shape may change its dimensions, but expression must not shrink teeth through linear interpolation. The maxillary arch remains attached to the cranium. Tongue and oral lining motion must be evaluated with the mandible and lip aperture, including conflicting combinations. Eye rotation must preserve globe dimensions; lids must maintain attachment and a coherent aperture. Shape-dependent placement must be evaluated before performance so a change of identity cannot leave a joint or an attached surface behind.

These are joint conditions for each anatomical group. A correction that clears one intersection while creating another, collapses a surface, changes a rigid organ's dimensions or removes the intended expression is not accepted.

## Primary sources under investigation

- [FLAME, Learning a model of facial shape and expression from 4D scans](https://download.is.tue.mpg.de/flame/flame_paper.pdf): separates identity, expression, articulated joints and pose-dependent corrections. Its learned representation is a reference method, not evidence that this repository has reproduced its behavior.
- [MetaHuman DNA, Rig Definition, and Rig Operation](https://dev.epicgames.com/documentation/metahuman/metahuman-dna-rig-definition-and-rig-operation): separates rig evaluation, joint transforms and geometry. Character-specific DNA geometry does not justify placing vertex fields in this editor's numerical documents.
- [In vivo three-dimensional mandibular kinematics and functional point trajectories](https://pmc.ncbi.nlm.nih.gov/articles/PMC7860955/): measures coupled rotation and translation. A fitted fixed screw is a geometric approximation; it is not automatically an anatomical temporomandibular joint model.
- [Pure rotation in the temporomandibular joint during jaw opening?](https://pmc.ncbi.nlm.nih.gov/articles/PMC11026373/): investigates the limits of a pure-rotation description. Numerical ranges require their population and measurement assumptions, rather than being presented as universal biological limits.

## Initial state

The investigation starts at master commit `d6be8fd9` after PR #2522. Its published documents still admit `identity` and `correctives`; its preview creates a worker for each request, rebuilds and validates a model, exports GLB, decodes GLB and replaces the scene. Measurements reported in #2533 belong to the previous environment and require local reproduction. The new work is delivered through one Draft PR.

## Canonical detail and simple controls

The user's additional requirement is tracked in [#2535](https://github.com/samchon/automovie/issues/2535). The finest numerical parameters are authoritative. Simple controls must resolve through explicit transformations to that representation; an intermediate tier is optional. Research includes proportions, outline, fullness, skeletal prominence, feature placement and shape, asymmetry, skin, hair and performance as well as age. A mode change must preserve detailed edits, and repeated application must not accumulate drift.

The [research inventory and references](https://github.com/samchon/automovie/issues/2535#issuecomment-5757654846) distinguish documented authoring methods from anatomical studies and local reproduction. [Body research issue #2536](https://github.com/samchon/automovie/issues/2536) separately preserves shared-control questions, sources and limits for a future checkout without `.wiki`. This PR's implementation scope is facial effects and their attachment boundaries only; it does not introduce a body editor or interpret facial authoring weights as measured whole-body attributes.

The [additional fine-control investigation](https://github.com/samchon/automovie/issues/2535#issuecomment-5761278974) records 69 native candidates and 130 extracted endpoints beyond the original regional inventory. The pinned MPFB source and actual Blender attachment refits reproduced all six neutral surfaces exactly, including zero recovery error after endpoint sampling. Candidates include separate medial/lateral eye-corner placement, regional lid height, epicanthal and lid-fold shape, Cupid's bow, regional lip height and facial outline. The curation and subsequent editor verification below select the facial inventory. Source names are interpreted from measured deformation rather than inherited as demographic labels or clinical ranges.

## Runtime investigation, 2026-09-21

### Fine curation and neutral neck boundary

All 131 native-candidate states, including neutral, were admitted and rendered at seven fixed views. All seventeen overview sheets were inspected. The curation in [fine-controls.json](fine-controls.json) retains 57 additional face-shape channels, including separate medial/lateral canthus and aperture controls, perioral regional controls, nasal placement and profile, and additive head outlines. Seven neck fields are retained for body coordination, four whole-head pose fields belong to articulation, and the native 58.293 mm forehead endpoint remains excluded pending proportional redesign. Angular ear endpoints are identified as stylization. Native weights are authored interpolation coordinates, with no clinical or population-range interpretation.

[fine-native-targets.json.gz](fine-native-targets.json.gz) preserves the measured shared endpoint fields, including excluded candidates. Per-surface SHA-256 digests use the exact neutral XYZ values serialized as little-endian Float64. The complete neutral correspondence and original neck connectivity are available in Git commit `0c75de1e377a7901ce4a8dd942d4718f27d32d8b`; all six recovered neutral surfaces matched the new extraction exactly. [prepare-fine-basis.ts](../../../../scripts/face-review/prepare-fine-basis.ts) reads those historical blobs and the compact documents at `e166c64a8cff9423d0924d00450f01ec8b964777`, adds the selected shared fields, and clips all endpoints using one set of affine stencils. Run it from `test/` with `pnpm exec ttsx -P tsconfig.scripts.json --no-plugins scripts/face-review/prepare-fine-basis.ts <new-output-directory>`. It prepares a separate review directory and does not publish it.

The neutral neck now intersects a common Y=-0.085 m plane instead of selecting whole triangles by centroid. The candidate has 16,942 skin vertices and 33,698 skin triangles. All 8,614 groom seats survive with unchanged barycentric positions; the eighteen unchanged subject weights preserve every other component position exactly. Actual editor captures cover all eighteen documents at rest, full jaw opening and full opening with mouth closure in seven views, 378 images. All eighteen sheets were inspected: the staircase boundary disappeared in the observed population, while pre-existing orbital ridges, stretched full-opening lips and groom/atlas defects remain. This is an open attachment boundary, with no body or cap. A deliberate reversal of the new intersection Y value failed the independent half-square-area assertion; exact source restoration passed.

The combined preparation admitted all eighteen documents and all 106 selected additional endpoints. It expands the basis to 141 shape channels and retains 52 expression channels. The simple map declares 26 groups, adding facial dimorphism, overall fullness, definition, cheek elevation, regional eye opening, outer-corner height, upper-lid fold height, nasal profile curvature, resting mouth corners, Cupid's bow, resting brow height and chin cleft. Their ranges remain residual-preserving authored coordinates. Every added fine channel carries its measured sign interpretation as literal editor text. [prepareFineBasisArtifacts.ts](../../../../scripts/face-review/prepareFineBasisArtifacts.ts) owns the numerical preparation separately from command I/O, so analytic square/attachment cases exercise its correspondence, remapping and refusal behavior without Git, Blender or filesystem tests.

### Expanded editor and export observations

Actual browser editing covered each of the 26 simple groups at -0.75 and +0.75 from neutral, 52 transactions and 364 colour/clay views. Every group member reached the requested normalized value; switching from simple to fine preserved the exact document. All 57 new descriptions matched the rendered text. Nine overview sheets and the independent red/green calibration were inspected on AMD Radeon 8060S through ANGLE/D3D11. The added controls visibly changed their declared regional forms. These endpoint observations do not establish all combinations or biological calibration.

All 18 unchanged numerical subject documents were exported through the actual editor button and reloaded with the installed GLTFLoader in the same scene. All pairs were inspected. At 790 by 850 pixels, the maximum absolute 8-bit channel differences in document order were `[1, 2, 1, 3, 40, 1, 1, 1, 1, 1, 1, 1, 10, 1, 30, 1, 1, 1]`. The three larger cases were probed through decoded attributes: every position, normal and UV value matched the resident GPU arrays exactly across all eight meshes. Checked material scalar fields and texture sampling settings also matched. The native path uses `MeshPhysicalMaterial`, while seven decoded materials use `MeshStandardMaterial`; the cause of the remaining pixel differences has not been isolated. The largest differences lie near hair boundaries. This is geometry/attribute preservation evidence with a stated rendering discrepancy, not pixel identity.

All 106 newly selected fine endpoints also passed the actual Float32 GLB exporter, with a byte count and SHA-256 recorded for each local result. A parsed comparison against compact commit `e166c64a8cff9423d0924d00450f01ec8b964777` found every value in all 18 documents unchanged except the required basis identity. Decimal formatting differences do not change those values. Shared rig coupling and the existing ocular/oral appearance defects remain separate acceptance work.

### Rigid component investigation

The candidate fits expression-deformed mandibular and left/right ocular components to their shape-only geometry with a proper unit-scale rotation and translation, and holds the maxillary component fixed. Independent SVD registration over neutral plus all 18 identities and 15 poses measured worst RMS residuals of `7.240e-13` mm for the mandibular arch, `3.084e-14` mm for the maxillary arch, and `3.512e-13`/`4.069e-13` mm for the globes. The preceding representation reached 0.599 mm for a mandibular pose and 0.167 mm for an outward gaze. This establishes component rigidity to floating precision, without establishing a physiological trajectory or tissue contact.

The ocular comparison holds the published 141-channel skin and attachments fixed while replacing only actual candidate teeth/globe buffers. It covers all 18 subjects, rest plus five gaze states, and seven views on both sides of the comparison: 1,512 images. All 18 overview sheets and calibration were inspected. No new exterior break was identified at this capture scale; the existing orbital and lid forms remain. A separate strict triangle-crossing census includes conjugate gaze and blink/gaze combinations, 198 states. Counts increase in 62 states, decrease in 95 and stay equal in 41. The largest increase is 697 to 763 in `generated-white-girl-01-connected` with conjugate right gaze; the largest decrease is 1,085 to 1,015 in `yoo-seung-ho-connected` with blink/down gaze. No previously noncrossing surface pair becomes crossing. These counts include the embedded orbital tissues, measure neither depth nor visible penetration, and are not an anatomical acceptance score.

An additional 1,260 images compare both conjugate horizontal gazes, blink, blink/up and blink/down across all 18 subjects and seven directions. All 18 sheets and the calibration were inspected. No new exterior rupture from the rigid candidate was identified at this scale. The frozen shared membership is preserved in [rigid-components.json](rigid-components.json): 705 vertices per globe, 1,884 mandibular vertices and 1,984 fixed maxillary vertices. Preparation verifies complete/disjoint membership through ordinary basis admission. The prepared `fine-141-rigid` basis equals the measured candidate exactly except its revision string; all 18 personal documents still match `e166c64a` numerically except that string.

The integrated revision was then exercised through the actual editor, without candidate-buffer substitution, for all 18 subjects at rest, opening with mouth closure and upward gaze. All 378 colour/clay frames, the 18 overview sheets and the calibration were inspected on the same hardware GPU. Across 108 teeth/globe mesh comparisons, the CPU position difference from the preceding Node evaluation was at most `3.469e-17` m and every Float32 position was identical. Normals differed by at most `2.137e-14`; twelve components rounded to different Float32 values, with maximum absolute difference `2.842e-14`. Indices and UVs were exactly identical. No new exterior break was identified at this scale. The existing orbital ridges, stretched mouth-closure forms and groom/atlas artifacts remain acceptance work. Exact cross-runtime Float64 identity is not claimed.

All 18 integrated subjects were also exported through the actual GLB button and reloaded into the same scene. All three comparison sheets and calibration were inspected. Maximum 8-bit channel differences were `[1, 2, 1, 13, 40, 1, 1, 1, 1, 1, 1, 1, 10, 1, 30, 1, 1, 1]`. The four cases above two levels were probed again: positions, normals and UVs matched exactly for all 32 resident/decoded mesh pairs, and the checked material scalars and texture sampling settings matched. The material-class difference and unresolved pixel discrepancy described above remain. Workspace build and both test-project compilations passed. The local full suite passed 1,328 of 1,329 scenarios; the only failure was the existing reference-admission scenario exceeding its 500 ms budget at 735 ms, with no assertion failure. A later isolated retry was interrupted during prolonged module preparation and supplies no timing verdict.

The preparation path was also run again for the preceding non-rigid 141-channel revision: all six output files had identical parsed values to the original preparation. A deliberate removal of groom-triangle rebinding failed the independent seat-ordinal assertion at `1 != 0`; exact source restoration passed. The current pure preparation tests use two sub-100 ms analytic scenarios, including native-field omissions, stale bindings, missing source triangles, unchanged personal values, input ownership, empty populations and a fixed component reaching the actual builder.

### First implemented coordinate map

The first coordinate map at `e166c64a` exposed fourteen groups: age structure, head width/height, facial fullness, cheek prominence, eye size/spacing, nose size, lip fullness, mouth width, chin projection, jaw width, ear size and under-eye fullness. Fine mode retained every shape and performance coordinate. The current 26-group map extends this initial view using the same conversion contract.

For each member weight `w`, normalization is `u = w / maximum` when nonnegative and `u = w / (-minimum)` when negative. The simple coordinate is the group's mean `s`; the residual is `r_i = u_i - s`. Changing to `t` resolves `u_i' = t + r_i`, followed by the signed inverse normalization. Its permitted interval is the intersection of each member's normalized interval minus `r_i`. Therefore paired differences and unlisted detail survive; an out-of-range input refuses rather than removing that detail. Each projection owns an origin and resolves every request from it. Returning to its original coordinate restores the exact saved values and omitted fields. A new projection after save/load recovers the coordinates to floating precision. No simple fields or per-vertex data are added to the document.

The browser uses the same numerical transaction/history owner for both modes. A mode or search change only changes presentation. Pending simple groups compose before evaluation; a refused edit discards the cancelled draft, and a following edit cannot revive it. A prototype-named coordinate exposed an inherited-property lookup defect in the first implementation; owned map lookup now distinguishes missing numeric values from object prototypes.

The age axis remains the existing authored `globalAgeStructure`, not years. In the actual browser, -0.8 and +0.8 visibly changed head proportions and orbital/midface form; they did not synthesize skin ageing or establish demographic calibration. On Radeon 8060S/ANGLE D3D11, three bald neutral age edits took 343–391ms. All eighteen compact subjects were recaptured at six colour and three clay directions; subject selection took 586–1016ms. All 162 images were inspected in six contact sheets. Narrow tooth bands, recurrent orbital ridges, groom attachment and opaque clay-card artifacts remain visible. Source-photo pose registration and the full expression population were not part of this capture; no likeness or anatomical completeness is accepted.

### Resident preview and export

Published documents now contain only numerical controls, material overrides and named resources. A structural comparison of all 18 documents confirmed that removing `identity` and `correctives` preserved every other field. Old geometry remains in Git history. This removal requires renewed anatomical population verification.

The connected preview now keeps its worker resident, sends numerical models directly, stages texture preparation and updates GPU buffers only at publication. GLB encoding is a separate request bound to the committed document at the export click. The fixed source-to-UV correspondence is compiled once. A resource's lookup key can differ from its internal ID; its exact basis binding must match the document.

An initial direct-render comparison exposed an inverted texture atlas: the native texture loader's default vertical flip differed from GLTFLoader. The adapter now preserves `flipY=false`, matching the installed GLTFLoader, and the refusal/recovery test checks the issued material's actual map. Repeated hardware captures of the first three subjects restored the expected eye, eyebrow, hair-mask and facial atlas placement. These observations verify the transport change's texture convention, not facial anatomy or likeness. Independent hand-authored red/green triangle markers confirmed the front-view positive-X/right convention before capture. Hardware was AMD Radeon 8060S through ANGLE/D3D11.

Initial resident browser measurements were approximately 0.8–1.0 seconds for subject selection and 1.1 seconds for a shape edit. They remain above the 50ms target. CPU profiling isolated repeated coordinate-string construction in topology validation: after compiling region correspondence, five full model evaluations took 423–507ms. Interning each evaluated welded vertex once reduced those evaluations to 125–212ms while retaining validation. A seeded differential probe compared 10,000 old/new topology verdicts, including their ordered diagnostics, without a mismatch. New unit examples independently pin nanometre welding, deformed vertex separation, all repeated-corner cases and diagnostic labels. Those CPU timings do not yet establish the complete browser latency after the validator change.

The actual Export GLB button was exercised for the first three subjects. Each downloaded file was parsed with the installed GLTFLoader and displayed in the same scene, camera, lighting and raster as its resident preview. At 790 by 850 pixels, 416, 605 and 221 pixels respectively differed; the largest channel difference was one 8-bit level. This is a sampled rendering comparison, not a proof over all Float32 inputs. Calibration markers and each pair of captures were inspected. The full local suite passed 1,313 tests. A deliberate reversal of the atlas convention made the renderer test fail at its material-map assertion; restoring the convention restored the pass.

Remaining work includes broader Float32 preview/export comparisons, expression coverage, a richer fine parameter inventory, age/shape interactions, anatomical joint trajectories and shared shape/expression corrections. Prepared groom resources are a possible performance improvement, subordinate to the user's parameter and anatomical priorities. No claim of physiological completeness follows from the numerical or transport checks.

### Final parameter replay checks

Review found that a three-member simple group with normalized values `[1, 0, 0]` could display its mean `1/3` outside its own feasible interval: subtracting the rounded residual put the calculated upper bound one floating step below the mean. The corresponding negative example had the same problem at the lower bound. The interval now explicitly contains its already admitted origin; outward edits still refuse, inward changes retain member differences, and no-op replay restores exact saved fields and omissions. The independent boundary test failed before this correction and passed afterwards. In the actual hardware browser, both nose-width endpoints with zero height/depth retained the exact document after reapplying the displayed simple value and switching modes. Calibration and both endpoint frames were inspected on Radeon 8060S through ANGLE/D3D11.

The prepared builder now validates its owned neutral structure and materials before returning, compares each edit's welded vertex partition, and takes the full model gate when a partition changes. One quantization owner serves both ordinary topology validation and reuse. Analytic examples cover valid merging, invalid same-winding superposition, nonfinite inputs, mutation isolation and invalid source materials. Disabling partition comparison made the split-vertex assertion fail; the original guard was restored. Five actual-subject CPU evaluations measured 76–96ms, independently of rendering. This timing is an observation, not the revised completion criterion.
