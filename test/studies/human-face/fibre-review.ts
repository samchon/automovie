import type * as Human from "@automovie/human";

/**
 * Connected-basis fibre pigment source inspection. The shared rule was read
 * beside its analytic card scenario, which pins the luminance-ratio colour,
 * the coverage factor, the cache and every refusal, and a real-builder
 * replay on the analytic contact basis. Neither the rule nor this record
 * accepts a subject's brow or lash likeness: pigment and density are
 * authored optical values under the renderer's light.
 *
 * @evidence requirements/actors/facial-authoring/contract.md#actor-face-review Keeps the fibre rule's source inspection separate from subjects' visual observations.
 * @evidence specifications/asset-and-representation/facial-authoring/contract.md#face-spec-connected-fibre Records source inspection of the coverage admission, luminance-ratio colour, coverage factor and cache of the fibre rule.
 *
 * @evidence {@link Human.createHumanFaceFibrePigment} Read the per-material decode, the drawn texels' median luminance, per-texel colour and coverage, the value-keyed cache and the texture and factor replacement.
 * @evidenceReview {@link Human.createHumanFaceFibrePigment} #4b8f77e Read overrides without pigment or density skipped before any lookup, ranges checked before the material, a material refused without an embedded texture or a mask or blend alpha mode, the decode kept per material and the painted texture cached by the pair of values, uncovered texels untouched, colour as the pigment times the texel's luminance over the median luminance of the texels the card draws (at or above a mask's cutoff, every covered texel when blended), held at one so the source's bright coverage fringes take the pigment itself, coverage multiplied and held to 255, and the base-colour factor set to white only when a pigment is given.
 */
export const humanFaceFibreReview =
  "Connected fibre pigment source inspection; subject likeness pending" as const;
