# 2026-09-26 사물 단계 지시와 실행

## 사용자 지시

- 단계 1 건물 작업을 중단하고 직전 커밋, lint, 건물 잔여만 확인해 `.wiki/단계1-잔여.md`로 인계한다. stale review는 대상 H2의 의미를 읽고 참인 행만 갱신한다.
- 18:30Z까지 models 사물 단계를 진행한다. 방별 모든 물건의 목록과 수를 `.wiki/사물-목록.md`에 먼저 만들고 마지막에 `N개 중 M개`로 보고한다.
- 재사용 prototype과 치수 변종을 쓰며, 한 물건에 15분 넘게 쓰지 않는다. 수치 owner를 한 곳에 두고 모든 prototype의 부품·주소·좌표를 문법 기반으로 검사한다. 모집단과 무작위 돌연변이 red 수를 출력한다. AST parser를 사용하지 않는다.
- `self-check` 한 명령에 모든 검사와 다섯 TypeScript 테스트 파일을 포함하고 각 종료 코드를 합산한다. 텍스처 비트맵은 만들지 않고 surface·scale·UV·fallback 결합을 기록한다.
- 기존 model-coordinate-audit와 model-part-audit 실패를 먼저 처리하고 `models` draft→evidence를 요청한다. 18:20Z에 이 production만 스테이지·커밋·push하고 18:30Z에 지정 형식으로 보고 후 총괄 리뷰로 넘어간다.

## 시작 상태

- 15:35Z 기준 작업 트리는 깨끗하고 최근 커밋은 `a6e001e1`, `3006d5ca`, `cd582a27`이다.
- `npm run lint` exit 0. stale review는 없다.
- 현재 `self-check.cjs`는 lint, 다섯 `.test.ts` 파일, 정규식 기반 `space-literal-audit.cjs`를 포함한 16개 검사를 열거하고 실패를 합산한다. 실제 전체 실행은 진행 중이다.
- 문서의 기존 models 모집단은 33 prototype, 91 state, 1,194 part다. source model은 아직 없다.

## 진행 기록

- 15:35Z 단계 1 잔여 항목을 목록으로만 기록했다. 건물 source는 변경하지 않았다.
- 16:05Z `model-coordinate-audit`의 축 좌표와 `model-part-audit`의 곡면 접촉 두 초기 실패를 닫았다. 바뀐 대상의 `@axis-control`·`@cap-contact`·`@cavity-contact` 행을 실점유와 비교했고 lint는 통과했다.
- 16:17Z 공간 15곳의 독립 사물 N=398과 종류 항목 196개를 `.wiki/사물-목록.md`에 연결했다. 모델 H2 43개·상태 159개·부품 1,262개 중 생활 소품은 H2 10개·상태 68개다. `model-owner-audit`의 모델 대응은 398/398이며 이 수는 설계 대응이지 실제 인스턴스·렌더 완료가 아니다.
- 16:29Z 새 소품의 일반 재료 결합 68개 상태와 분리 마감 면 7개를 `docs/materials/001-binding-and-scale.md#surface-bindings`에 기록했다. texture scale은 기존 직물·목재 owner를 인용하며, 나머지는 무texture 단색 응답이다. 결합 누락은 주소 오류로 실패하도록 별도 `model-surface-audit`에서 검사한다. texture 비트맵은 만들지 않았다.
- 문장 문자열과 특정 ID 목록에 맞춘 이전 `model-design-audit`를 전수 H2 완결성 문법 검사로 교체하고 `model-prose-table-audit`를 제거했다. 기존 특수 산문 비교의 감소는 좌표·부품 전수 검사와 설계 Self-Review에서 다시 검토한다. legacy child 대응의 ID 변환은 아직 이전 검사에 남아 있으므로 R4 전체 충족 여부는 최종 검토에서 별도로 밝힌다.
- 16:29Z 전체 `self-check` 재실행 중이다. 이 커밋의 `models` 선언은 아직 `draft`이고 판정 요청은 `draft→evidence`다. graph stage 자체를 선행 변경하지 않는다.
- 16:32Z 사물 목록의 `398`은 합성 모델 내부 부품 5개를 독립 instance로 중복 셌음이 드러나 `393`으로 정정됐다. 원 목록과 정정 사유를 `.wiki/사물-목록.md`에 모두 보존했다. 최초 398/398 관찰은 당시 목록의 대응 결과였고 이 정정 시점의 분모·분자는 393/393이었다.
- 16:36Z 코트 두 벌의 현관 걸림점으로 독립 코트 걸이 1개가 빠진 것을 확인해 `wall-accessories/coat-hook` 상태와 방별 목록을 추가했다. 당시 모집단은 394개다. 후속 검토에서 작업용/거실 화면, 실내/실외 매트, 실내/실외 폐기통을 별도 상태에 연결하고 여분 침구를 접힌 수납 묶음으로 정리했다.
- 16:43Z 전체 `self-check`에서 17개 중 15개가 통과하고 표면 감사 파일의 모듈 부재로 2개 호출이 실패했다. 같은 표면·scale·UV·fallback 검사는 추적된 `model-owner-audit.cjs`에 통합되어 71개 상태, 71개 part, 31개 binding과 비선택 행 삭제 1/1 red를 출력한다. 따라서 중복·누락된 별도 호출을 `self-check`에서 제거한 현재 목록은 15개다. 이 변경 뒤 전체 명령을 다시 실행한다.
- `.wiki/`는 production `.gitignore`에서 제외되지만 사용자는 `단계1-잔여.md`를 유일한 인계 기록으로 지정하고 커밋되지 않은 것은 없다고 명시했다. 두 기록을 정확한 경로로 강제 스테이징해 이 production 커밋에 포함한다. 다른 production의 동시 변경은 스테이지 또는 커밋하지 않는다.
- 16:55Z 기존 room source의 현관 화분, 세 침실의 화면·키보드, 주방 분리수거장이 빠진 것을 확인했다. 방별 상태·치수 변종 연결도 구분해 당시 독립 사물 모집단은 **402개**, 설계 대응은 **402/402**였다. 주침실 러그는 거실용 대신 기존 `rugs/bedroom1600x2200` 상태에 연결했다.
- 17:00Z 수치 owner 재검토에서 물병 재료의 광학 thickness와 모델 벽 두께를 분리해 적고, 접시·팬을 포함한 열린 식기의 중공 반지름과 바닥 높이는 해당 `@bore` 행만 소유하게 산문을 바로잡았다. 표·산문의 중복 수치가 다른 형상을 지시하지 않도록 했다. 이 시점의 `self-check`는 15검사 0실패였으나 실행 중 목록이 395개에서 402개로 확장돼 최종 검사는 다시 실행한다.
- 17:05Z room source의 천장 광점 25개와 기존 매입등 계획 12개의 관계를 대조했다. 12개 중 8개는 source에 있고 4개는 추가 계획이어서, 빠진 source 광점 17개와 별도 상부장 선형등 1개를 더해 현재 모델 설계 목록은 **420개 중 420개 대응**이다. 전체 `self-check`는 44 prototype·1,266 part를 대상으로 15검사 0실패였고 부재 무작위 변형은 7개 중 7개가 red였다. 실제 instances·modelSources의 검증 완료 수는 0개다.
- 17:14Z 방별 원본 `×n` 목록을 단일 개수 원천으로 삼고 기존 `model-owner-audit.cjs`가 목록의 N·M·방별 요약·총계가 계산값과 일치하는지도 검사하도록 넓혔다. N, M, 총계, 방별 소계 변경과 요약 행 삭제의 다섯 변이는 모두 red였다. 현 목록은 방 15곳·물체 종류 행 212개·독립 물체 420개이며 설계 상태 대응 420개다.
- 17:20Z 같은 검사가 `src/house/plan.ts`의 실제 방 ID 12개를 사물 목록과 대조한다. 무작위로 선택한 방의 목록 행을 제거하면 1/1 red였다. 전체 self-check 중 `model-part-audit.cjs`의 자동 포맷이 JSDoc 타입 단언을 바꿔 TypeScript 오류 95개를 냈고, 포맷 전 의미 동일 파일을 복구한 뒤 `npm run lint` exit 0을 확인했다. 포맷본은 저장소 밖에 보관했고 전체 self-check를 다시 실행 중이다.
- 17:12Z 새 물체 문서를 다시 읽어 자전거 바퀴의 두께 축을 X 폭에서 Z 반두께 `@axis-control`로 바로잡았다. 상부장 선형등의 밑면 접촉 변형은 특정 prototype ID를 고르지 않고 접촉 행 모집단에서 무작위 선택하며 red를 확인했다. 이 뒤 최신 전체 검사는 다시 실행한다.
- 17:15Z 방별 목록의 선언 수치 검사를 산문 문장 문자열 대신 `@census spaces/objects/covered` 문법으로 옮겼다. 행 목록과 방별 소계·합계를 독립 계수하고 선언을 틀리게 바꾼 음성 검사를 포함한다. 이 변경을 반영한 전체 검사는 다시 실행한다.
- 17:21Z `model-part-audit.cjs` 전체를 포맷하면 기존 inline JSDoc 단언이 망가져 lint 95건을 만드는 것을 확인했다. 같은 내용이 이미 HEAD에 들어온 것을 대조해 그 파일만 HEAD의 유효한 형식으로 복원했고, 이후 lint exit 0을 확인했다. `model-owner-audit.cjs`의 표면 결합 책임은 별도 `model-surface-bindings.cjs`로 분리해 포맷 뒤에도 두 파일이 각각 500줄 이내다. 방별 소유자·표면 음성 검사는 통과했고 최신 전체 검사는 다시 실행한다.
- 17:33Z 설정의 작업실 접이식 작업면·숨은 수납·반투명 screen을 소유 설계와 대조했다. 각각 `work-desk/aux-panel`, 책상·수납침대·책장의 부품, 외피 창 하부 반투명 유리여서 독립 사물 420개에 중복 가산하지 않는다는 범위를 목록 첫머리에 명시했다.
- 17:38Z 용기 뚜껑의 필수 여부를 산문에 `@vessel-closure` 문자열이 있는지로 고르던 분기를 제거하고, 모든 타원 cavity profile에 구조적 closure 행이 있어야 한다는 검사로 바꿨다. lint exit 0이며 삭제 변형을 포함한 부품 fixture는 재실행 중이다.
- 17:26Z 주전자·커피 기구의 주둥이와 손잡이 비율 선언을 `docs/models/005-everyday-objects.md`의 한 행으로 통일했다. 처음 작성된 독립 `model-vessel-audit.cjs`는 사용자 지시인 기존 검사 확장에 맞춰 `model-part-audit.cjs` 내부의 검사·무작위 변이로 합치고 self-check의 별도 두 호출을 제거했다. 44 prototype·1,266 part·용기 2상태 감사와 lint가 exit 0이다.
- 17:29Z 소품 표면의 finish 이름이 실제 응답 정의로 귀결되는지도 기존 `model-owner-audit.cjs`가 쓰는 `model-surface-bindings.cjs` helper에 합쳤다. 새 독립 검사는 남기지 않았다. 생활 소품 결합 33행에서 정의 15개와 참조 18개가 해결됐고, 무작위 10행의 finish 이름 훼손은 10/10 red였다. 용기 선언 여섯 필드의 무작위 훼손도 기존 부품 감사 fixture에서 6/6 red였다.
- 17:43Z 용기 뚜껑의 선언 삭제·높이 이탈·무작위 병 목 확대를 기존 `model-part-audit.cjs --fixture`에 합쳐 3/3 red를 확인했다. `npm run self-check`는 15검사 0실패였다. 독립 뚜껑 감사 파일과 호출은 제거해 기존 검사 모집단을 넓히라는 지시를 따른다.
- 17:48Z 주방의 벽 조리대·쿡탑에는 배기 후드의 모델 상태가 없음을 찾아 독립 사물 1개를 추가했다. 목록은 15구역 **421개 중 421개 설계 대응**이고 모델은 45 prototype·164상태·1,269부품이다. `kitchen-extractor/default`의 상부장 접촉·좌우 필터·중앙 틈과 표면 주소를 모델·재료 표에 결합했다. 실제 배관과 환기 성능은 범위 밖이며 메시·배치는 후속 단계다. 현재 전체 self-check를 재실행 중이다.
- 17:54Z 후드를 반영한 `npm run self-check`는 15검사 0실패였다. 주소 감사는 45 prototype·164상태·1,269부품에서 무작위 행 삭제 10/10 red이고, 표면 감사는 결합 40행·면 선언 14개와 응답 범위 변형을 읽었다. 부품 감사의 비선택 변형 7/7, 용기 8/8, 뚜껑 3/3 red도 통과했다. `model-owner-audit.cjs`가 새 검사 추가로 500줄 제한을 넘어서 마감 응답 변형 검사만 표면 결합 helper로 옮겼고 두 파일은 각각 496줄·171줄, lint exit 0이다.
- 18:01Z 새 배기 후드와 종전의 1.6m짜리 상부장 광띠는 같은 2.9m 폭 상부장 밑에서 나란히 맞지 않는다. 광띠를 표의 0.8m 상태 하나로 줄이고 좌우 두 instance가 후드 바깥에 붙도록 설계했다. 두 광띠와 후드의 폭 합계는 2.24m여서 상부장 폭 안에 여유가 있으며 정확한 개별 transform과 비충돌 검증은 instances에 넘긴다. 방별 목록을 15구역 **422개 중 422개 설계 대응**으로 고쳤다. 모델 주소·부품·좌표·사물 소유 감사는 새 모집단에서 모두 exit 0이고 전체 self-check를 다시 실행한다.
- 18:09Z 새 생활 사물 H2 12개와 방 15구역의 물체 종류 213행·422개를 저작 경계로 다시 읽었다. 기존 33 H2까지 합한 구조 감사 모집단은 설계 H2 45개·164상태·1,269부품, 생활 사물 재료 결합 40행·분리 면 선언 14개다. 사용자의 사물 범위·재사용·수치 소유·기존 검사 확장 지시와 `principles/design/models.md`·`obligations/design/models.md`의 모델 경계를 대조했다. 찾은 주방 배기 후드 누락과 단일 광띠 간섭을 위 두 변경으로 갚은 뒤 새 생활 사물의 목록·모델·재료·검사 결과를 다시 읽었고 추가로 확정한 누락은 없었다. `npm run self-check`는 15검사 0실패, 임의 행 삭제 주소 10/10, 부품 7/7, 용기 부착 8/8, 용기 마개 3/3 red다. 설계의 문법·주소 검증이며 기존 33 H2의 이 경계에서의 재독해와 실제 mesh·배치·재료 source·렌더 판정은 포함하지 않는다. 모델 단계는 아직 `draft`로 남겨 판정을 요청한다.
- 18:12Z 개발 규칙의 500물리줄 제한을 대조했다. 이번에 확장한 `model-part-audit.cjs`는 현재 2,128줄로 여전히 한 파일에 너무 크며 `npm run lint`는 이 규칙을 검사하지 않는다. `model-owner-audit.cjs`의 496줄은 새 응답 변형 검사를 helper로 옮겨 제한 안으로 되돌렸으나, 부품 감사의 책임 분할은 이 마감에서 해결되지 않았다. 따라서 `models`의 evidence 전이 준비 여부를 검토할 때 이 규칙 부채를 함께 보고하고 문법 검사 green을 전체 변경 준수로 표현하지 않는다.
- 17:54Z 고정된 421개 목록에 대한 `npm run self-check`는 15검사 0실패였다. 45 prototype·164상태·1,269부품의 구조 계측과 전 부품 5,076개 음성 검사, 비선택 무작위 부재 변이 7/7 red, 용기 부착 8/8 red, 닫힘 3/3 red, 표면 결합 40행을 확인했다. 이 통과는 모델 설계와 감사의 범위다. 산문 소수 1,525개 중 축 좌표 740개의 증인만 확인됐고 비축 785개는 전수 일치가 미확인이다. 기존 `model-child-audit.cjs`에는 ID별 legacy 대응 분기가 남아 있어 사용자 R4의 문법 전수 원칙을 전체 검사군에 대해 충족했다고 주장하지 않는다. native modelSources·instances·materialSources의 새 물체 실체 및 화면은 아직 0개다.
- 18:15Z 마감 대상은 목록 15구역·422개 중 모델 설계 대응 422개, 45 prototype·164상태·1,269부품이다. 최종 `npm run self-check`는 15검사 0실패, 전 부품 변형 5,076건, 비선택 무작위 부재 7/7 red였고 `npm run lint` exit 0으로 stale review 0건이다. 설계 미대응 물체는 0개다. 판정에 함께 넘길 미해결 사항은 축 문법 밖 산문 수치 785개, 기존 ID별 legacy 검사 분기, 2,128줄 부품 감사의 500줄 규칙 초과, native modelSources·instances·materialSources와 새 사물 렌더 미구현이다. 동시 작업의 `public/textures/*.png` 7개와 건물·뷰어 source 변경은 이 사물 단계의 커밋 대상에서 제외한다. `models`는 `draft`로 유지하고 18:30Z에 `draft→evidence` 판정을 요청한다.

## 18:30Z 이후 총괄 리뷰 — 고정 기준 1차

- 판정 기준은 library / complete-production, Git `c5820829cde593cf8d76b769ad115eda74240d8e`의 별도 detached worktree, viewer source SHA-256 `21693f8eb4e772cfafa30076f949a69955b70780c9c275d408cf087da28b68e6`, Chromium 1600×1000·DPR 1·FOV 50, AMD Radeon 8060S Direct3D11 WebGL이다. 다섯 사용자 reference의 2026-09-16 첨부본을 비교 대상으로 사용했다. 공유 작업 트리의 동시 수정과 그 뒤 커밋은 이 관찰 기준에 포함되지 않는다.
- 낮/작업의 비조립상세 관찰점 190개를 열거했고 188개를 실제 GPU 축소 화면(각 300×180, 10개 contact sheet)으로 열어 봤다. `child-bedroom-1/corner-0`과 `upper-service/corner-3`은 pose가 없어 열 수 없었다. 외관·4입면·지붕·하부·개구부·내부 방들의 여러 방향·다섯 reference 대응 화면이 이 축소 조사에 포함된다. 조립상세 관찰점 859개와 독립 모델 중립 정면/우측/상부/45° 화면은 검사하지 못했다. 축소 조사를 모든 세부의 전체 화소 판정으로 승격하지 않는다.
- 실제 GPU 전면 입면과 작업실 문턱을 낮/작업, 사적/손님, 야간/손님 세 상태에서 원래 canvas 크기로 따로 열어 봤다. renderer는 AMD Radeon 8060S, 브라우저 page error 0이다. 차폐 상태 변화는 전면 유리와 작업실 창에서 보인다. 여섯 named state의 `/scene` 응답은 모두 HTTP 200, `audit.errors=[]`; 낮/작업은 element 4,558·native model 526·placement 5,776, 손님 상태는 4,566·526·5,783이다. 이 숫자는 기존 방 source의 임시 가구와 건물 요소를 포함한다.
- **총괄 납품 차단:** `docs/models`의 45 H2·164 상태·1,269 part와 방별 422개 대응은 설계다. 이 커밋의 `src` 아래 `models`와 `instances`가 없고 `buildHouse()`는 방 source를 직접 조립한다. 따라서 새 소품 목록을 실제 독립 model/instance source로 옮겨 정확한 수량·위치·part/face·재료 결합을 생성·관찰했다는 증거는 없다. 기존 native model 526개를 새 422개가 구현됐다는 수로 읽지 않는다. 방 화면에는 책상·침대·주방 같은 임시 fit-out이 있으나 목록의 작은 식기·직물·외부 비품 완결은 검증할 수 없다.
- **필수 관찰 실패:** 위 두 방 모서리는 `docs/settings/001-production.md#delivery-review-condition`이 요구하는 관찰 분모에 들어간다. pose가 없다는 이유가 기록돼 있어도 실제 화소 판정은 불가능하다. 계단 연속 원통 통과, headroom, 설비·구조 성능도 선언대로 unverified다.
- **사물 검증 한계:** `npm run self-check` 15검사 0실패는 설계 문법 결과다. 산문 수치 1,525개 중 축 좌표 740개만 증인으로 검증했고 비축 785개는 전수 확인이 아니다. inherited `model-child-audit.cjs`의 ID별 분기와 `model-part-audit.cjs` 2,128줄은 사용자 R4 및 개발 파일 길이 규칙의 미해결 부채다. 무작위 부재 변형 7/7 red와 총 5,076개의 부재 변형 검사는 native 메시의 부재·좌표·중립 렌더를 대신하지 않는다.
- 사용자 지시의 사물 단계에서는 bitmap 제작을 금했으나 별도 동시 작성자 커밋 `28b49156`에 `public/textures/*.png` 7개가 포함됐다. 본 세션의 단계 2 커밋 `1b502291`은 그 이미지를 작성하지 않았다. 현재 전달물의 규칙 충돌로 기록하고 이 리뷰에서 생성·삭제하지 않는다.
- 현재 `models` 선언은 `draft`다. `draft→evidence`를 요청했으나 승인이나 전진 선언은 아니다. complete-production 총괄 리뷰의 깨끗한 전수 라운드는 0회다. 위 필수 구현·관찰·지시 충돌이 해소되지 않아 종료/승인을 주장하지 않는다. 이후 공유 브랜치의 source 수정은 새 기준으로 다시 관찰해야 한다.
- 추가로 `flex-workroom/corner-2`와 `center-z-plus`를 낮/작업·낮/손님에서 같은 GPU 조건으로 원본 canvas 크기로 대조했다. 닫힌 수납침대 전면이 열린 침대·베개 형상으로 바뀌는 것은 실제 화소에서 보인다. 이는 기존 `src/house/rooms/flex.ts`의 임시 상태 구현 관찰이며 새 models H2의 `murphy-bed` native 구현 판정이 아니다.

- 18:50Z 지시 연대기 정정: 위의 “bitmap 제작 금지와 7개 PNG가 충돌한다”는 판단은 최신 사용자 지시를 반영하지 못했다. 사용자는 17:5xZ에 텍스처 이미지 제작을 금지가 아닌 권장으로 명시하고 이전의 비트맵 범위 밖 규칙을 철회했다. 따라서 `28b49156`의 절차 생성 PNG 7장은 현재 지시와 충돌하지 않는다. 이전 단계 2 지시는 이 최신 직접 지시에 의해 해당 범위에서 대체되었고, 앞 기록의 당시 판단은 남겨 둔다.
- 18:55Z 같은 고정 basis에서 reference 관찰점 `01-exterior`, `02-section-axonometric`, `03-common-room`, `04-flex-room`, `05-upper-private-floor` 다섯 화면을 1263×815 원본 canvas 크기로 열어 사용자 2026-09-16 이미지와 대조했다. 절개·공용부·작업실·상층 공간 관계는 blocking pass로 읽힌다. 식탁의 그릇·식기, 작업실 소품, 상층 침구와 외부 비품의 새 모델 구현은 이 화면에서 확인되지 않는다. ref02는 절개 검사 화면이지 사진 수준의 납품 영상이 아니다.
- 19:00Z 재료 결합 경계 재검토: 현재 `src/house/assembly.ts`는 `stone`·`plaster`·`oak`·`felt`·`linen`·`tile`·`soil`·`paving`에 `materialTexture()`를 결합하고 `src/viewer/payload.ts`는 7개 절차 생성 RGBA 자원을 전달한다. 반면 `README.md#현재-재료-전달-범위`의 “pv만 texture”, “나머지 roughness 0.76 단색”, “재료 source 미구현” 설명은 이 커밋의 source와 다르다. `docs/materials/007-observation.md#material-state-samples`의 여섯 상태 영향 면·문 상태 전체 시각 검사는 이번 표본에서 끝나지 않았다. 따라서 재료 시각 완료도 주장하지 않는다.
- 19:11Z 두 번째 고정 기준 `63a995a2`, source basis `6a3bc089db1f094dd10670f0625eace69f9b36f7cab133c6da36d2f65d33a8fa`, Chromium 1600×1000·DPR 1·AMD Radeon 8060S Direct3D11에서 낮/작업의 비조립상세 188개 contact sheet 10장과 reference 대응 원본 화면 5장을 다시 캡처했다. 첫 기준 `c5820829`의 대응 PNG와 각각 byte 동일해 그 화면의 앞선 관찰을 그대로 재현했다. `63a995a2`의 여섯 named state `/scene` 응답은 모두 HTTP 200·`audit.errors=[]`였다. 상태 두 선택값을 연속 변경한 최초 화면은 선택값과 다른 상태로 보였으므로 아래처럼 개별 요청을 완료시켜 검증했다.
- 19:11Z 뷰어 상태 전환 경합을 확인했다. `src/viewer/client.mjs`의 두 selector change handler가 각각 `load()`를 시작하며 완료 순서 또는 요청 토큰을 검사하지 않는다. Playwright에서 `private/work` 요청만 2.5초 지연하고 곧바로 `private/guest`를 완료시킨 뒤 전면 입면을 캡처하자 UI 선택값은 `private/guest`였으나 PNG SHA-256 `68709485...`는 순차적으로 완료한 `private/work` 화면의 SHA와 정확히 같았다. 순차 전환한 `private/guest` 화면 SHA는 `fe3b8c36...`로 달랐다. 이 결과는 네트워크 응답 순서가 뒤집힐 때 이전 payload가 마지막에 업로드되는 재현 가능한 관찰이다. 건물·뷰어 단계는 15:30Z에 닫혔으므로 이 세션에서 해당 source를 수정하지 않고 총괄 리뷰 발견으로 인계한다. 이 경합 때문에 UI select 상태만으로 여섯 상태의 시각 판정을 주장할 수 없다.
- 19:17Z 최신 공유 브랜치에서 `npm run self-check`를 재실행하니 15검사 중 1실패(exit 1)였다. `npm run lint`와 다섯 TS 테스트 파일·6 export 및 나머지 감사는 통과했고 `model-coordinate-audit.cjs`가 산문 소수 1,525개 중 축 증인 740개 밖에 남은 785개를 35 prototype H2의 측정 문법 외 값으로 red 처리했다. `f73c0d34`가 이 이전부터 알려진 한계를 성공 출력에서 실패로 승격시킨 결과다. 18:15Z 단계 2 커밋 `1b502291`의 당시 15/0 결과를 소급해서 바꾸지 않되 현재 `models:draft→evidence` 전이 근거는 더 부족해졌다. 같은 최신 브랜치의 README 재료 설명은 `397c5ac4`와 `7b8ee0ee`에서 고쳐져 위 19:00Z에 기록한 문서 불일치는 현재 해결됐다.
- 19:22Z `npm run self-check -- --bench` 합산 명령은 22검사 중 4실패(exit 1)였다. 위 좌표 감사 외에 외부 `doc-review-numbers`가 수치 후보를 반환했고, `docs/models` review 및 재료→model face 인용 탐침은 대상 0건을 무검사 실패로 집계했다. anchor 341개는 끊김 0개, shared reason 탐침은 중복 0개였다. 외부 수치 출력은 확인할 후보 목록이지 각 행의 결함 확정이 아니며, 두 0모집단을 통과로 읽지 않는다. 이 결과는 다른 작성자의 단계 3 source 감사 기록과 일치한다.
- 19:26Z 고정 basis `63a995a2`의 여섯 `/scene` 상태를 `day/work`와 구조 비교했다. guest는 닫힌 작업실 패널 2 element·placement를 빼고 침대 계열 10 element·9 placement를 넣는다. private·night는 기본과 같은 opening topology를 유지하면서 유리 model 2개와 shade element·placement 74개의 값을 바꾼다. 이 차이는 네트워크 응답의 구조 사실이며 실제 화면의 픽셀 판정을 대체하지 않는다.
- 19:26Z 같은 AMD GPU·1600×1000 viewer에서 두 selector의 요청을 각각 완료시킨 다음 privacy 3 × flex 2의 6조합마다 영향 창 10곳을 300×180 축소 화면으로 캡처해 여섯 contact sheet의 실제 픽셀을 열었다(총 60창 표본, 브라우저 page error 0). 낮은 실내가 유리 뒤로 보이고, 사적 상태는 위쪽 밝은 shade와 아래쪽 어두운 투과 band, 야간 상태는 전폐된 밝은 shade로 구별된다. 차양과 프레임의 자리도 유지된다. 축소 화면에서는 인접 재료 이음·정확한 UV 규모·guest 침구의 part/face나 문 grain 움직임을 판정할 수 없으며 현재 catalog source 부재와 두 관찰점 미가용 때문에 재료·전체 납품 완료로 해석하지 않는다.
- 19:30Z 누락의 실제 소비 경로를 더 좁혀 읽었다. `buildHouse()`는 `garden()`과 `common()`을 직접 부르고 둘 모두 임시 `Assembly` 부재를 만든다. `src/house/site/garden.ts`는 대지·식재·배수구를 만들지만 방별 목록의 자전거·거치대·우편함·택배함·야외 의자·탁자·쓰레기통을 만들지 않는다. `src/house/rooms/common.ts`는 식탁·주방 장과 주요 설비를 만들지만 목록의 식기·조리 소도구를 생성하지 않는다. 고정 `/scene`의 element ID에서도 bicycle/mail/parcel/waste/fork/spoon이 0개다. 이름 검색은 단독 부재 증명이 아니므로 source 호출과 화면 관찰을 결합한 예시로만 적고, 기존 소파 베개 7개처럼 이미 구현된 임시 물체까지 없는 것으로 세지 않는다.
- 19:34Z 재료 설계와 현재 자원 사이의 구체적 차이도 확인했다. `docs/materials/004-wood.md#oak-floor`는 `oak-grain` 1024²를, `006-wet-and-joinery.md#wet-tile`은 `tile-grain` 512²를, `005-soft-finishes.md#felt-panels`는 `felt-grain` 512²를 정한다. 현재 `makeTextureAssets()`와 `public/textures` PNG 헤더는 oak/tile 각 256²이며 `felt-grain`은 없고 felt는 `woven-grain`에 결합된다. `006-wet-and-joinery.md#worktop-stone`의 `worktop-grain` 512²도 현재 자원·결합에 없다. 또한 `001-binding-and-scale.md#metric-texture-coordinates`는 무채색 grain의 선형 평균을 요구하나 현재 바인딩은 sRGB로 해석하며 생성 byte의 `limestone-grain` 선형 평균은 약 .929(설계 .98), `oak-grain`은 약 .905(설계 .96)다. 이 수치는 생성 RGBA 전체의 sRGB→linear 변환 평균이다. 현재 `materials:evidence` 단계의 부분 source 전달을 완결 판정할 수 없는 구체적 사례로 남긴다.
