# 단계 1 건물 잔여 인계

기준: 2026-09-25 15:35Z, `benchmark/1951-1953-harness`, 직전 세 커밋 `a6e001e1`, `3006d5ca`, `cd582a27`. 이 단계에서는 건물 source와 계약을 수정하지 않는다.

- `npm run lint`: exit 0. 현재 stale `@evidence*Review` 진단 0건, 그 밖의 lint 진단 0건. 따라서 지문을 갱신할 대상이 없다.
- 현재 트리 전체에 대한 GPU 화면, 다섯 reference, 모든 건축 관찰 분모의 재판정은 미완이다. 과거 일부 관찰과 현재 수정 트리의 완료 판정은 구분한다 (`docs/spaces/002-spatial-graph.md#spatial-verification`).
- 계단 headroom, 되돌아 나오는 동선의 clearance, 가상 보행 원통의 연속 통과는 현재 문서에서 `unverified`다 (`docs/spaces/002-spatial-graph.md`, `docs/settings/002-household.md`).
- gross 264㎡, 계단 구멍을 뺀 바닥, 내부 유효 면적의 별도 산출물 계측은 미완이다 (`docs/spaces/002-spatial-graph.md`).
- 구조·법규·에너지·실제 설비 성능·피난·인체 사용성은 검사와 인증을 받지 않았다 (`docs/settings/002-household.md#program-boundary`).
- 물체·재료 이관 전의 방 source에는 임시 물체와 material 문자열이 남아 있다. 해당 이관은 사물 및 후속 분기의 일이며 건물 단계의 수정으로 취급하지 않는다 (`docs/settings/003-spatial-basis.md#surface-decomposition`).
