# 단계 1 건물 잔여 인계

기준: 2026-09-25 15:35Z, `git log --oneline -3`의 선두 `a6e001e1`; `npm run lint` exit 0, stale `@evidenceReview` 0건. 이 문서는 건물 단계의 남은 항목만 기록하며 단계 2에서 건물은 수정하지 않는다.

- `spaceSources`는 `draft`다. 현재 뷰어는 `buildHouseEnvironment(buildHouse())`의 공간 부재만 받으므로 모델 다중 부품, 실제 재료 면 결합 및 UV를 보여주지 못한다. 건물 메시와 텍스처의 실제 GPU 검증은 미완료다.
- 이전 v-123 기록의 모델 접촉, 문선 이웃, 걸레받이 이웃, 예약 초과, 린넨장 패널과 레인지 내부, 치수 미비는 모델 증거 단계의 후속 검증 대상이다. 설계 문장과 실제 메시의 일치는 아직 검증되지 않았다.
- 이전 작업 기록에서 `npm run check`는 재료 면 증거 후보, 접촉 문장, reviewed spaces 명사의 미분류 후보 때문에 red였다. 이는 lint 오류가 아니다. 다음 총괄 리뷰에서 각각 실제 부재와 검사의 관계를 다시 판단해야 한다.
- 참조 이미지 대비 건물 외관·방 관찰은 구조적 수치 검사만으로 확정되지 않는다. 이미지 기준 프레임의 시각 판정은 미완료로 인계한다.
